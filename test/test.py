import math
import os
import time
import json

d = json.loads('/Users/Mille/GDriveMBD/Term3/NLP/Group_Project/aplus_faq_dict.json')

print(d)


#data = faqs.keys()
#chunks = [list(data)[x:x+100] for x in range(0, len(data), 100)]
#chunks = [data.get(x, x+100) for x in range (0, len(data), 100)]
#print(chunks)

#chunks = [list(data)]

#chunks = [data[x:x+100] for x in range(0, len(data), 100)]

#print(chunks)
